{
  "name": "flash-cards",
  "version": "0.1.0",
  "private": true,
  "description": "Hugo-powered static flash-card site for CompTIA study decks.",
  "scripts": {
    "test": "npm run validate",
    "validate": "node scripts/validate-package-scripts.js && node scripts/validate-theme-selector.js && node scripts/validate-reset-progress.js && node scripts/validate-deck-data.js && hugo --minify && node scripts/validate-export.js && node scripts/browser-smoke.js",
    "browser:smoke": "node scripts/browser-smoke.js",
    "build": "hugo --minify",
    "serve": "hugo server --bind 127.0.0.1 --port 3000 --baseURL http://127.0.0.1:3000/ --disableFastRender --renderToMemory"
  },
  "engines": {
    "node": ">=20"
  }
}
